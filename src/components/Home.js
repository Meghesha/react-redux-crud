import React from 'react';

const Home = ()=>{
    return(
        <React.Fragment>
            <h2>React JS CRUD using REDUX & JSON Server</h2>
        </React.Fragment>
    )
}

export default Home;